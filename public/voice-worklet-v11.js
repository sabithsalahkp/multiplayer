class PlayVerseVoiceCapture extends AudioWorkletProcessor{constructor(){super();this.samples=[];this.targetRate=16000;this.chunkFrames=4096;}emitChunk(){if(this.samples.length<this.chunkFrames)return;const input=Float32Array.from(this.samples.splice(0,this.chunkFrames));const ratio=sampleRate/this.targetRate;const outLen=Math.max(1,Math.floor(input.length/ratio));const out=new Int16Array(outLen);let pos=0;for(let i=0;i<outLen;i++){const next=Math.min(input.length,Math.floor((i+1)*ratio));let sum=0,count=0;for(;pos<next;pos++){sum+=input[pos];count++;}const s=Math.max(-1,Math.min(1,count?sum/count:0));out[i]=s<0?s*32768:s*32767;}this.port.postMessage({pcm:out.buffer},[out.buffer]);}process(inputs,outputs){const input=inputs[0]?.[0];if(input?.length){for(let i=0;i<input.length;i++)this.samples.push(input[i]);while(this.samples.length>=this.chunkFrames)this.emitChunk();}const out=outputs[0]?.[0];if(out)out.fill(0);return true;}}registerProcessor('playverse-voice-capture',PlayVerseVoiceCapture);
